# Скопируйте в .env и заполните: cp .env.example .env
# Для Docker: рабочая директория в контейнере — /app, данные в /app/data (volume ./data:/app/data)

# Папка с данными (в Docker = /app/data). Если файлы уже есть — предобработка не запускается
RAG_DATA_DIR=data
# Папка с PDF для парсинга (по умолчанию; можно переопределить в POST /preprocess/parse)
INPUT_DIR=data/docs
DATASET_JSON=data/dataset.json
OUTPUT_EMBEDDINGS=data/embeddings.npy
OUTPUT_META=data/embeddings_meta.json
FAISS_INDEX=data/faiss.index
BM25_CORPUS_JSON=data/bm25_corpus.json

# Yandex Cloud (обязательно для LLM)
YANDEX_CLOUD_FOLDER=
YANDEX_CLOUD_API_KEY=
YANDEX_CLOUD_MODEL=qwen2.5-7b-instruct/latest

# Модель эмбеддингов. В Docker без сети используйте e5-base (предзагружен в образ); e5-large нужен интернет на первый запуск
EMBED_MODEL=intfloat/multilingual-e5-large

# API (Docker: API_HOST=0.0.0.0)
API_HOST=0.0.0.0
API_PORT=8000
